---
phase: 03-content-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _bibliography/papers.bib
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
must_haves:
  truths:
    - "Each of the four papers in _bibliography/papers.bib has a note: field with a one-sentence plain-language description"
    - "The bib.liquid template renders entry.note inline under the venue/year line for every entry that has a note: field"
    - "The bio on _pages/about.md states the PhD was completed (with honors) — no 'completing a PhD' or in-progress phrasing remains anywhere in the codebase"
    - "The CV data file _data/cv.yml states the PhD graduated with honors in 2025"
    - "The news directory contains entries covering the 2024–2025 milestones (ACL 2024, NAACL 2024, CLiC-it 2024, ACL 2025, FBK appointment, PhD defense) and no _news/*.md file declares a custom permalink: front matter"
    - "Every public page (about, publications, projects, news, cv) has a description: front-matter field that metadata.liquid renders into <meta name=\"description\">, og:description, and twitter:description tags"
  artifacts:
    - path: "_bibliography/papers.bib"
      provides: "Bibliography entries with plain-language note: descriptions for all four papers"
      contains: "note ="
  key_links:
    - from: "_bibliography/papers.bib"
      to: "_layouts/bib.liquid"
      via: "Jekyll-Scholar entry.note rendering at line 190"
      pattern: "entry\\.note"
    - from: "_pages/*.md (front-matter description:)"
      to: "_includes/metadata.liquid"
      via: "page.description used in <meta>, og:description, twitter:description"
      pattern: "page\\.description"
---

<objective>
Phase 3 closes all four content-accuracy requirements (CONT-01 through CONT-04). Three of the four are pre-answered as already correct in the codebase per D-01, D-05, D-06, D-07 — they are verified, not modified. CONT-03 is the only requirement requiring code edits: add a one-sentence plain-language `note:` field to each of the four BibTeX entries in `_bibliography/papers.bib`, written in the research-value-focused voice established by the featured cards on `_pages/publications.md`.

Purpose: Visitors to /publications/ see a plain-language summary inline with each paper, not just venue/year/abstract toggle. Existing bio, news, and SEO meta remain unchanged because they are already factually correct (verified by Phase 3 discovery).

Output: An updated `_bibliography/papers.bib` containing four `note = {…}` fields (one per entry), and a documented verification confirming bio/news/SEO already satisfy CONT-01, CONT-02, CONT-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-accuracy/03-CONTEXT.md
@.planning/phases/03-content-accuracy/03-PATTERNS.md
@_bibliography/papers.bib
@_pages/publications.md

<interfaces>
<!-- Where note: gets rendered in the bibliography template. Treat bib.liquid as READ-ONLY. -->

From _layouts/bib.liquid lines 186–191:
```liquid
<div class="periodical">
  {{ periodical | strip }}
</div>
<div class="periodical">
  {{ entry.note | strip }}
</div>
```

Behavior: The `note:` field value is rendered as plain text in a `.periodical` div directly after the venue/year line. No HTML, no italics, no links — write the value as a plain English sentence. Curly-brace delimiters `{...}` are required (consistent with all other string fields in the file). BibTeX handles internal commas, colons, and em-dashes inside `{...}` without escaping.

From _includes/metadata.liquid (already-wired SEO path — verification only, NOT modified in this phase):
```liquid
<meta name="description" content="{{ page.description }}">
<meta property="og:description" content="{{ page.description }}">
<meta name="twitter:description" content="{{ page.description }}">
```

Behavior: Any page with `description:` front-matter is automatically wired into all three social/SEO meta tags. No template change needed for CONT-04.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add plain-language note: field to all four BibTeX entries</name>
  <files>_bibliography/papers.bib</files>
  <read_first>
    - _bibliography/papers.bib (read the entire file before editing — see all four entries and the existing field-spacing convention)
    - _pages/publications.md (lines 19–35: confirm the featured-card voice that the new notes must match)
    - .planning/phases/03-content-accuracy/03-PATTERNS.md (lines 58–115: voice rules and per-paper drafted notes)
    - _layouts/bib.liquid (lines 186–191: confirm entry.note is already rendered — no template change needed)
  </read_first>
  <action>
For each of the four `@inproceedings{...}` entries in `_bibliography/papers.bib`, add exactly one new field of the form:

```
  note = {SENTENCE.},
```

Place the new `note =` line immediately AFTER the existing `bibtex_show = {true},` line and BEFORE the `abstract = {...}` line (or before the closing `}` if there is no abstract). Use curly-brace delimiters consistent with every other string field in the file. End the sentence with a period inside the braces. Add a trailing comma after the closing brace (matches sibling fields like `bibtex_show = {true},`).

Use these EXACT note: values (one full English sentence each, lead with the research object, no jargon like "transformer/LLM/SVM/UmBERTo", no first-person "We"):

**Entry `occhipinti:etal:2020italianlp` (EVALITA 2020) — add:**
```
  note = {Author profiling in Italian — an early study on inferring speaker attributes from text, and the system that won every subtask.},
```

**Entry `occhipinti-etal-2025-superman` (ACL 2025) — add:**
```
  note = {Explores how dialogue generation changes when models know not just who is speaking, but who they are speaking to — and what that relationship means for consistency.},
```

**Entry `occhipinti-etal-2024-fine` (ACL 2024 / HED-IT) — add:**
```
  note = {Investigates how human post-editing of machine-generated dialogues affects model training, finding that data quality matters most for smaller models.},
```

**Entry `occhipinti:etal-2024-prodigy` (NAACL 2024 / PRODIGy) — add:**
```
  note = {Introduces PRODIGy, a dataset pairing movie-script dialogues with rich speaker profiles — personality, biography, and communication style — to support persona-aware dialogue generation.},
```

Notes on field placement:
- The EVALITA 2020 entry uses two-space indentation. Match that. The other three entries use four-space indentation. Match that. Do not normalize spacing — preserve each entry's existing convention.
- Do NOT modify any existing field (title, author, abstract, url, doi, abbr, preview, selected, bibtex_show, code, html, etc.). Only ADD the new `note =` line.
- Do NOT alter blank lines between entries.
- Do NOT change the keys (`occhipinti:etal:2020italianlp`, etc.).

This satisfies D-02, D-03, D-04 — adds a `note:` to every paper (not only the three selected) using the research-value-focused voice from publications.md featured cards.
  </action>
  <verify>
    <automated>cd /Users/daniela/Desktop/website/danie08.github.io && test "$(grep -c '^[[:space:]]*note = {' _bibliography/papers.bib)" = "4" && grep -q 'Author profiling in Italian' _bibliography/papers.bib && grep -q 'who they are speaking to' _bibliography/papers.bib && grep -q 'human post-editing of machine-generated dialogues' _bibliography/papers.bib && grep -q 'Introduces PRODIGy' _bibliography/papers.bib && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^[[:space:]]*note = {' _bibliography/papers.bib` outputs exactly `4`
    - `grep -c '@inproceedings{' _bibliography/papers.bib` still outputs exactly `4` (no entries added or removed)
    - `_bibliography/papers.bib` contains the literal string `note = {Author profiling in Italian — an early study on inferring speaker attributes from text, and the system that won every subtask.},`
    - `_bibliography/papers.bib` contains the literal string `note = {Explores how dialogue generation changes when models know not just who is speaking, but who they are speaking to — and what that relationship means for consistency.},`
    - `_bibliography/papers.bib` contains the literal string `note = {Investigates how human post-editing of machine-generated dialogues affects model training, finding that data quality matters most for smaller models.},`
    - `_bibliography/papers.bib` contains the literal string `note = {Introduces PRODIGy, a dataset pairing movie-script dialogues with rich speaker profiles — personality, biography, and communication style — to support persona-aware dialogue generation.},`
    - For each `note =` field, the line immediately above is `bibtex_show = {true},` (verifiable by reading the file)
    - `grep -E '\bUmBERTo\b|\bSVM\b|\bLLM\b|\bfine-tuning\b' _bibliography/papers.bib | grep '^[[:space:]]*note = {'` returns no results (jargon-free notes per voice rules)
    - `grep -E '^[[:space:]]*note = \{(We |This paper )' _bibliography/papers.bib` returns no results (notes do not start with "We" or "This paper" per voice rules)
  </acceptance_criteria>
  <done>All four BibTeX entries in `_bibliography/papers.bib` have a `note = {…}` field placed after `bibtex_show = {true},`. The four note sentences match the exact text specified. No existing field has been modified. CONT-03 is satisfied.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify CONT-01, CONT-02, CONT-04 are already satisfied — record evidence in summary</name>
  <files>_pages/about.md, _data/cv.yml, _news/, _pages/publications.md, _pages/projects.md, _pages/news.md, _pages/cv.md, _includes/metadata.liquid</files>
  <read_first>
    - _pages/about.md (read entire file — confirm PhD phrasing per CONT-02 / D-01)
    - _data/cv.yml (read entire file — confirm "graduated with honors" 2025 phrasing per D-01)
    - _includes/metadata.liquid (read entire file — confirm page.description is rendered into description, og:description, twitter:description per D-06)
  </read_first>
  <action>
This task does NOT modify any file. It runs verification commands proving the three pre-answered requirements (CONT-01, CONT-02, CONT-04) remain satisfied as documented in `03-CONTEXT.md` (D-01, D-05, D-06, D-07), and records the evidence so the phase summary can cite it.

Run each verification command listed in `<acceptance_criteria>` below. If any command fails, STOP and report — do not silently fix. The expectation set by Phase 3 discovery is that these all pass without modification. A failure means the discovery snapshot is stale and the user must be informed before any edit.

Capture the output of each command (exit codes and matched strings) for inclusion in the phase SUMMARY.md `verification` section. The SUMMARY must explicitly cite:
- CONT-01: which `_news/*.md` files cover ACL 2024, NAACL 2024, CLiC-it 2024, ACL 2025, FBK appointment, PhD defense
- CONT-02: the exact line in `_pages/about.md` and `_data/cv.yml` confirming "completed PhD" / "graduated with honors" phrasing
- CONT-04: the list of pages with `description:` front-matter and the line in `metadata.liquid` that renders them

Do NOT add, edit, or delete any front-matter, news entry, or template line in this task. The task's deliverable is verification evidence, not code change.
  </action>
  <verify>
    <automated>cd /Users/daniela/Desktop/website/danie08.github.io && grep -q "completed a PhD" _pages/about.md && ! grep -qE 'completing a PhD|currently a PhD|PhD candidate|pursuing a PhD' _pages/about.md _data/cv.yml && grep -q "graduated with honors" _data/cv.yml && grep -lq "ACL 2025" _news/*.md && grep -lq "ACL 2024" _news/*.md && grep -lq "NAACL 2024" _news/*.md && ! grep -E '^permalink:' _news/*.md && grep -q 'page.description' _includes/metadata.liquid && grep -q '^description:' _pages/about.md && grep -q '^description:' _pages/publications.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "completed a PhD" _pages/about.md` returns at least `1` (CONT-02 / D-01)
    - `grep -E 'completing a PhD|currently a PhD|PhD candidate|pursuing a PhD' _pages/about.md _data/cv.yml` returns NO results (no stale in-progress phrasing remains anywhere)
    - `grep -c "graduated with honors" _data/cv.yml` returns at least `1` (CONT-02 / D-01)
    - `grep -l "ACL 2025" _news/*.md` lists at least one news file (CONT-01 / D-05)
    - `grep -l "ACL 2024" _news/*.md` lists at least one news file (CONT-01 / D-05)
    - `grep -l "NAACL 2024" _news/*.md` lists at least one news file (CONT-01 / D-05)
    - `grep -l "PhD" _news/*.md` lists at least one news file covering the PhD defense milestone (CONT-01 / D-05)
    - `grep -E '^permalink:' _news/*.md` returns NO results (CONT-01: no broken custom permalinks per D-05)
    - `grep -c 'page.description' _includes/metadata.liquid` returns at least `3` (CONT-04: rendered into description + og:description + twitter:description per D-06)
    - `grep -l '^description:' _pages/about.md _pages/publications.md _pages/projects.md _pages/news.md _pages/cv.md` lists every existing page file (CONT-04 / D-06; only check files that exist — do not fail if a page like _pages/news.md is absent)
    - The phase SUMMARY.md (written at end of phase by execute-phase) cites CONT-01, CONT-02, CONT-04 with the file paths and grep evidence above
  </acceptance_criteria>
  <done>All three verification commands pass with the expected output. CONT-01, CONT-02, CONT-04 are confirmed already satisfied. Evidence is captured for the phase SUMMARY.md `verification` section. No file in the codebase has been modified by this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Site author → static site → public visitor | Content authored by Daniela is rendered as HTML at build time and served as a static page. No user input crosses any trust boundary at runtime. |
| Build pipeline (GitHub Actions) → public CDN | Jekyll build transforms `_bibliography/papers.bib` into HTML; only the build output is served. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `_bibliography/papers.bib` `note:` field content | accept | Static text authored by site owner; no untrusted input is ever interpolated into the field. BibTeX `{...}` delimiters render as plain text via `_layouts/bib.liquid`'s `entry.note | strip` filter — no HTML evaluation. |
| T-03-02 | Information Disclosure | `note:` field text leaking unpublished research details | accept | All four entries describe already-published peer-reviewed papers; the note: text adds no information beyond the public abstract. Reviewed by site owner before commit. |
| T-03-03 | Denial of Service | Malformed BibTeX breaking Jekyll-Scholar build | mitigate | Acceptance criteria require `grep -c '@inproceedings{'` to remain `4`, ensuring no entry was accidentally truncated. GitHub Actions Jekyll build (Ruby 3.3.5) acts as the build-time validator — a syntax error fails CI before deploy. |
| T-03-04 | Tampering | Stale or incorrect bio/PhD/news verification evidence | mitigate | Task 2's grep gates fail-closed: any drift from the discovery-snapshot expectations causes the task to fail, surfacing the discrepancy before commit. No silent "self-heal" is permitted. |
| T-03-05 | Spoofing / Information Disclosure | Social-media link previews showing stale or wrong title/description | accept | CONT-04 is satisfied by existing front-matter `description:` fields; verified, not modified. Future page additions are out of scope for this phase. |

</threat_model>

<verification>
Phase-level checks:
1. `cd /Users/daniela/Desktop/website/danie08.github.io && grep -c '^[[:space:]]*note = {' _bibliography/papers.bib` → must be `4`
2. `cd /Users/daniela/Desktop/website/danie08.github.io && grep -c '@inproceedings{' _bibliography/papers.bib` → must still be `4` (no entries added or removed)
3. `cd /Users/daniela/Desktop/website/danie08.github.io && bundle exec jekyll build --trace` (if Ruby toolchain present locally) — must complete without `Liquid Exception` or `BibTeX` errors. Otherwise rely on GitHub Actions deployment job for build validation.
4. Optionally serve locally and visit `/publications/` — each non-featured paper should display its plain-language note on a new line directly under the venue/year text.
</verification>

<success_criteria>
- `_bibliography/papers.bib` contains exactly 4 `note = {…}` fields, one per `@inproceedings{` entry, each placed immediately after `bibtex_show = {true},`.
- Each note: value matches the exact text specified in Task 1 (research-value-focused, one full sentence, no jargon, does not start with "We" or "This paper").
- No other file in the repo is modified by this phase (Task 2 is verification-only).
- CONT-01, CONT-02, CONT-03, CONT-04 are all satisfied — three by existing state, one by the bib.bib edit.
- GitHub Actions deployment passes on the resulting commit (Ruby 3.3.5 Jekyll build succeeds).
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-accuracy/03-01-SUMMARY.md` documenting:
- The exact text of the four `note:` values added (cite line numbers in `_bibliography/papers.bib`)
- The verification evidence for CONT-01, CONT-02, CONT-04 (grep output and matched files)
- Any deviations from the suggested text in `03-PATTERNS.md` (and the voice-rule reasoning if so)
- Confirmation that `_layouts/bib.liquid` was NOT modified (it is read-only for this phase)
</output>
