---
phase: 01-visual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _sass/_variables.scss
  - _sass/_themes.scss
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "Every page renders with a warm off-white (#FAFAF8) background, not pure white"
    - "Theme accent color is indigo-plum (#5C4B8A), no longer the al-folio blue (#0076df)"
    - "Body text reads in near-black (#1A1A1A) on the warm off-white background"
    - "All `var(--global-*)` consumers across the site automatically pick up the new palette"
    - "Dark mode tokens are paired for every new light-mode token (dark mode is live: `enable_darkmode: true`)"
  artifacts:
    - path: "_sass/_variables.scss"
      provides: "SCSS color and font primitives ($plum-color, $warm-white, etc.)"
      contains: "$plum-color: #5C4B8A"
    - path: "_sass/_themes.scss"
      provides: "Updated semantic CSS tokens for light and dark mode"
      contains: "--global-theme-color: #{$plum-color}"
  key_links:
    - from: "_sass/_variables.scss"
      to: "_sass/_themes.scss"
      via: "SCSS variable interpolation `#{$plum-color}` etc."
      pattern: "\\$plum-color|\\$warm-white|\\$warm-black"
    - from: "_sass/_themes.scss"
      to: "compiled _site/assets/css/main.css"
      via: "Sass build of main.scss"
      pattern: "5C4B8A|FAFAF8"
---

<objective>
Replace the al-folio default blue palette with the locked indigo-plum + warm off-white palette, propagated through the existing two-tier SCSS token system. This is the foundation step — Plans 02 and 03 build on these tokens.

Purpose: VIS-01. Distinctive, scholarly color identity replaces the generic blue. Because 183+ `var(--global-*)` consumers already exist in `_base.scss`, updating the tokens propagates the new palette site-wide automatically — no component edits needed.

Output: Updated `_sass/_variables.scss` (new color primitives added; existing primitives preserved) and `_sass/_themes.scss` (semantic tokens repointed in both `:root` and `html[data-theme="dark"]` blocks; new font-stack tokens declared as placeholders for Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation/01-CONTEXT.md
@.planning/phases/01-visual-foundation/01-RESEARCH.md
@.planning/phases/01-visual-foundation/01-PATTERNS.md
@.planning/phases/01-visual-foundation/01-UI-SPEC.md
@CLAUDE.md

<critical_constraints>
1. `enable_darkmode: true` is live in `_config.yml` (line 433). Every NEW light-mode token MUST have a paired dark-mode value. Do NOT disable dark mode.
2. Do NOT remove `$blue-color`, `$red-color`, `$green-color`, `$grey-color`, `$grey-color-dark`, `$grey-900`, `$white-color` from `_variables.scss` — they are used elsewhere (alert blocks, award badges, code backgrounds, back-to-top button).
3. Do NOT hardcode hex values in component SCSS. Component SCSS already references `var(--global-*)` tokens — leave them alone.
4. The `@use "sass:color"` import already exists at line 6 of `_variables.scss` — do not duplicate it.
5. Light-mode `--global-card-bg-color` keeps the value `#FFFFFF` (literal). Source attribution `#{$white-color}` is acceptable; do not switch it to a plum value.
</critical_constraints>

<interfaces>
SCSS variables consumed downstream (already used in `_themes.scss` lines 7–122):
- Existing functional primitives PRESERVED: `$blue-color` (#0076df), `$red-color`, `$red-color-dark`, `$green-color`, `$grey-color` (#767676), `$grey-color-light`, `$grey-color-dark` (#1c1c1d), `$grey-900` (#212529), `$white-color` (#ffffff), `$black-color` (#000000)
- Derived token: `$code-bg-color-light: rgba($blue-color, 0.05)` at line 37 — UPDATE to `rgba($plum-color, 0.05)` per UI-SPEC Open Questions resolution
- New primitives consumed by `_themes.scss`: `$plum-color`, `$plum-light`, `$plum-bg-dark`, `$plum-card-dark`, `$warm-white`, `$warm-black`, `$warm-grey`, `$warm-grey-light`, `$warm-text-dark`
- New font primitives consumed by `_themes.scss` (Plan 02 applies them in `_layout.scss` / `_base.scss`): `$font-sans`, `$font-serif`

CSS custom properties exposed to all component SCSS:
- `--global-bg-color`, `--global-text-color`, `--global-text-color-light`, `--global-theme-color`, `--global-hover-color`, `--global-divider-color`, `--global-card-bg-color`, `--global-footer-bg-color` — all updated
- `--font-sans`, `--font-serif` — new tokens (consumed in Plan 02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add palette and font-stack primitives to _variables.scss</name>
  <files>_sass/_variables.scss</files>
  <read_first>
    - _sass/_variables.scss (the file being modified — read full file to see exact line numbers for color block, design tokens block, spacing scale, and the `$code-bg-color-light` derivation)
    - .planning/phases/01-visual-foundation/01-PATTERNS.md (the `_sass/_variables.scss` section — has exact insertion points and the full block to add)
    - .planning/phases/01-visual-foundation/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-10 for locked values)
  </read_first>
  <action>
Insert new SCSS primitives in `_sass/_variables.scss`. Two insertion blocks — DO NOT remove or edit any existing variables.

INSERTION 1 — palette primitives. Insert immediately AFTER `$black-color: #000000 !default;` (currently around line 33) and BEFORE the `// Theme colors` comment / `$code-bg-color-light` line:

```scss
// Palette — indigo-plum (Phase 1 Visual Foundation)
$plum-color: #5C4B8A;
$plum-light: #9B8CC4;
$plum-bg-dark: #1C1A24;
$plum-card-dark: #252330;
$warm-white: #FAFAF8;
$warm-black: #1A1A1A;
$warm-grey: #5E5A6E;
$warm-grey-light: #9B95AA;
$warm-text-dark: #E8E3F0;
```

INSERTION 2 — font-stack primitives. Insert immediately AFTER the radii block (after `$radius-pill: 999px;` — currently around line 51), and BEFORE the spacing scale (`$space-1: 0.25rem;`):

```scss
// Font stacks (Phase 1 Visual Foundation)
$font-sans: "Inter", system-ui, -apple-system, sans-serif;
$font-serif: "Source Serif 4", "Georgia", serif;
```

EDIT — update the `$code-bg-color-light` derivation. Find the line currently reading `$code-bg-color-light: rgba($blue-color, 0.05);` (around line 37 — it now sits AFTER your INSERTION 1). Change `$blue-color` to `$plum-color`:

```scss
$code-bg-color-light: rgba($plum-color, 0.05);
```

DO NOT touch any other lines. Specifically:
- Keep `$blue-color: #0076df !default;` exactly as-is — it is used by `_themes.scss` for functional purposes elsewhere
- Keep all greys, reds, greens, oranges, cyans, purples
- Keep `$code-bg-color-dark: #2c3237 !default;`
- Keep all `$space-*`, `$text-*`, `$radius-*` tokens
- Keep the existing `@use "sass:color";` at the top — do not duplicate
  </action>
  <verify>
    <automated>grep -E '^\$plum-color: #5C4B8A;$' _sass/_variables.scss && grep -E '^\$warm-white: #FAFAF8;$' _sass/_variables.scss && grep -E '^\$font-sans: "Inter"' _sass/_variables.scss && grep -E '^\$font-serif: "Source Serif 4"' _sass/_variables.scss && grep -E '^\$code-bg-color-light: rgba\(\$plum-color, 0\.05\);$' _sass/_variables.scss && grep -E '^\$blue-color: #0076df !default;$' _sass/_variables.scss</automated>
  </verify>
  <acceptance_criteria>
    - `_sass/_variables.scss` contains line `$plum-color: #5C4B8A;`
    - `_sass/_variables.scss` contains line `$plum-light: #9B8CC4;`
    - `_sass/_variables.scss` contains line `$plum-bg-dark: #1C1A24;`
    - `_sass/_variables.scss` contains line `$plum-card-dark: #252330;`
    - `_sass/_variables.scss` contains line `$warm-white: #FAFAF8;`
    - `_sass/_variables.scss` contains line `$warm-black: #1A1A1A;`
    - `_sass/_variables.scss` contains line `$warm-grey: #5E5A6E;`
    - `_sass/_variables.scss` contains line `$warm-grey-light: #9B95AA;`
    - `_sass/_variables.scss` contains line `$warm-text-dark: #E8E3F0;`
    - `_sass/_variables.scss` contains line `$font-sans: "Inter", system-ui, -apple-system, sans-serif;`
    - `_sass/_variables.scss` contains line `$font-serif: "Source Serif 4", "Georgia", serif;`
    - `_sass/_variables.scss` contains line `$code-bg-color-light: rgba($plum-color, 0.05);` (changed from `$blue-color`)
    - `_sass/_variables.scss` still contains line `$blue-color: #0076df !default;` (preserved)
    - `_sass/_variables.scss` still contains line `$white-color: #ffffff !default;` (preserved)
    - `_sass/_variables.scss` still contains line `$black-color: #000000 !default;` (preserved)
    - `_sass/_variables.scss` still contains the spacing scale `$space-1: 0.25rem;` through `$space-12: 3rem;`
    - Sass compiles without errors: `bundle exec jekyll build` succeeds
  </acceptance_criteria>
  <done>New palette and font-stack primitives are declared in `_variables.scss`; the existing `$code-bg-color-light` derivation is repointed to `$plum-color`; all preserved variables remain unchanged; Sass build compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint semantic tokens in _themes.scss for both :root and html[data-theme="dark"]</name>
  <files>_sass/_themes.scss</files>
  <read_first>
    - _sass/_themes.scss (the file being modified — full read; the `:root` block runs roughly lines 7–76 and `html[data-theme="dark"]` runs roughly lines 78–122; confirm exact line numbers before editing)
    - _sass/_variables.scss (verify the new primitives from Task 1 are in place — these are the values being interpolated)
    - .planning/phases/01-visual-foundation/01-PATTERNS.md (the `_sass/_themes.scss` section — has the full token-substitution table for both blocks)
    - .planning/phases/01-visual-foundation/01-UI-SPEC.md (Token Implementation Map and Color section for the locked values)
  </read_first>
  <action>
Make EXACTLY these substitutions inside the existing `:root { ... }` and `html[data-theme="dark"] { ... }` blocks in `_sass/_themes.scss`. Do not add or remove any other tokens; do not reorder declarations; do not change tokens listed in the PRESERVE column of the table below.

A) In the `:root { ... }` block (light mode), update these declarations IN PLACE:

| Token line (current → new) |
|----------------------------|
| `--global-bg-color: #{$white-color};` → `--global-bg-color: #{$warm-white};` |
| `--global-text-color: #{$black-color};` → `--global-text-color: #{$warm-black};` |
| `--global-text-color-light: #{$grey-color};` → `--global-text-color-light: #{$warm-grey};` |
| `--global-theme-color: #{$blue-color};` → `--global-theme-color: #{$plum-color};` |
| `--global-hover-color: #{$blue-color};` → `--global-hover-color: #{$plum-color};` |
| `--global-footer-bg-color: #{$grey-color-dark};` → `--global-footer-bg-color: #{$plum-bg-dark};` |
| `--global-divider-color: rgba(0, 0, 0, 0.1);` → `--global-divider-color: rgba(92, 75, 138, 0.12);` |

B) In the `:root { ... }` block, ADD these two new declarations (place them at the end of the block, immediately before the closing `}` or before any nested rule like `#light-toggle-system`):

```scss
--font-sans: #{$font-sans};
--font-serif: #{$font-serif};
```

C) In the `html[data-theme="dark"] { ... }` block (dark mode), update these declarations IN PLACE:

| Token line (current → new) |
|----------------------------|
| `--global-bg-color: #{$grey-color-dark};` → `--global-bg-color: #{$plum-bg-dark};` |
| `--global-text-color: #{$grey-color-light};` → `--global-text-color: #{$warm-text-dark};` |
| `--global-text-color-light: #{$grey-color};` → `--global-text-color-light: #{$warm-grey-light};` |
| `--global-theme-color: #{$blue-color};` → `--global-theme-color: #{$plum-light};` |
| `--global-hover-color: #{$blue-color};` → `--global-hover-color: #{$plum-light};` |
| `--global-footer-bg-color: #{$grey-900};` → `--global-footer-bg-color: #{$plum-bg-dark};` |
| `--global-divider-color: #424246;` → `--global-divider-color: rgba(155, 140, 196, 0.15);` |
| `--global-card-bg-color: #{$grey-900};` → `--global-card-bg-color: #{$plum-card-dark};` |

D) In the `html[data-theme="dark"] { ... }` block, ADD these two new declarations at the end of the block (mandatory dark-mode pair for the new font tokens — fonts don't change with theme but the tokens must be present):

```scss
--font-sans: #{$font-sans};
--font-serif: #{$font-serif};
```

PRESERVE — leave these tokens untouched in BOTH blocks (do not change source or value):
- `--global-code-bg-color` (light: `#{$code-bg-color-light}` — its derivation already updated in Task 1; dark: `#{$code-bg-color-dark}`)
- `--global-hover-text-color` (`#{$white-color}` in both blocks)
- `--global-footer-text-color`, `--global-footer-link-color`, `--global-distill-app-color`
- `--global-card-bg-color` in `:root` (currently `#{$white-color}` — leave as-is)
- `--global-highlight-color` (`#{$red-color-dark}` — award badge color)
- `--global-back-to-top-bg-color`, `--global-back-to-top-text-color`
- All newsletter and alert-block tokens (`--global-tip-block*`, `--global-warning-block*`, `--global-danger-block*`)

DO NOT touch any nested rules (e.g. `#light-toggle-system`, `.distill`) inside `_themes.scss`.
  </action>
  <verify>
    <automated>cd /Users/daniela/Desktop/website/danie08.github.io && bundle exec jekyll build 2>&1 | tail -20 && grep -F -- '--global-theme-color: #{$plum-color};' _sass/_themes.scss && grep -F -- '--global-bg-color: #{$warm-white};' _sass/_themes.scss && grep -F -- '--global-bg-color: #{$plum-bg-dark};' _sass/_themes.scss && grep -F -- '--font-sans: #{$font-sans};' _sass/_themes.scss && [ "$(grep -cF -- '--font-sans: #{$font-sans};' _sass/_themes.scss)" = "2" ] && grep -i "5C4B8A\|5c4b8a" _site/assets/css/main.css | head -3 && grep -i "FAFAF8\|fafaf8" _site/assets/css/main.css | head -3 && (grep -E "0076df|0076DF" _site/assets/css/main.css | grep -v -i "code\|rgba" | head -3 || echo "blue absent from non-code tokens")</automated>
  </verify>
  <acceptance_criteria>
    - `_sass/_themes.scss` contains line `--global-theme-color: #{$plum-color};` (in `:root` block)
    - `_sass/_themes.scss` contains line `--global-hover-color: #{$plum-color};`
    - `_sass/_themes.scss` contains line `--global-bg-color: #{$warm-white};`
    - `_sass/_themes.scss` contains line `--global-text-color: #{$warm-black};`
    - `_sass/_themes.scss` contains line `--global-text-color-light: #{$warm-grey};`
    - `_sass/_themes.scss` contains line `--global-footer-bg-color: #{$plum-bg-dark};` (appears at least once for `:root`; same value reused in dark block)
    - `_sass/_themes.scss` contains line `--global-divider-color: rgba(92, 75, 138, 0.12);`
    - `_sass/_themes.scss` contains line `--global-bg-color: #{$plum-bg-dark};` (in `html[data-theme="dark"]` block)
    - `_sass/_themes.scss` contains line `--global-text-color: #{$warm-text-dark};`
    - `_sass/_themes.scss` contains line `--global-theme-color: #{$plum-light};`
    - `_sass/_themes.scss` contains line `--global-card-bg-color: #{$plum-card-dark};`
    - `_sass/_themes.scss` contains line `--global-divider-color: rgba(155, 140, 196, 0.15);`
    - `_sass/_themes.scss` contains line `--font-sans: #{$font-sans};` exactly TWICE (once in each block)
    - `_sass/_themes.scss` contains line `--font-serif: #{$font-serif};` exactly TWICE (once in each block)
    - `_sass/_themes.scss` does NOT contain `--global-theme-color: #{$blue-color}` (confirms blue token is gone)
    - `_sass/_themes.scss` does NOT contain `--global-bg-color: #{$white-color}` (confirms warm-white token applied)
    - Preserved tokens still present: `_sass/_themes.scss` contains `--global-highlight-color: #{$red-color-dark};` and `--global-hover-text-color: #{$white-color};`
    - `bundle exec jekyll build` succeeds (no Sass compile errors)
    - Compiled `_site/assets/css/main.css` contains the literal string `5C4B8A` (case-insensitive)
    - Compiled `_site/assets/css/main.css` contains the literal string `FAFAF8` (case-insensitive)
    - Compiled `_site/assets/css/main.css` does NOT contain `0076df` outside of any residual code-bg derivation (run `grep "0076df" _site/assets/css/main.css | grep -v -i "code"` returns no lines, OR returns only the `$code-bg-color` rgba derivation if Sass keeps the literal expansion — note: the Task 1 update should eliminate even this residue)
  </acceptance_criteria>
  <done>Both light-mode and dark-mode CSS custom properties are repointed to plum/warm primitives; new `--font-sans` and `--font-serif` tokens declared in both blocks; preserved functional tokens (alert blocks, highlight color, back-to-top, etc.) untouched; `bundle exec jekyll build` produces a `_site/assets/css/main.css` containing `5C4B8A` and `FAFAF8` and free of the al-folio blue token.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline → static asset | `_site/assets/css/main.css` is generated from SCSS at build time and served as a public asset |
| Browser → CSS parser | Untrusted browsers parse the served CSS; malformed CSS could degrade UX but not breach security boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `_sass/_variables.scss` SCSS primitives | accept | Static site, no runtime input. SCSS values are committed to git. Tampering requires repository write access — out of scope for ASVS L1 on a public personal site. |
| T-01-01-02 | Information Disclosure | `_themes.scss` CSS tokens | accept | Tokens contain only color and font-stack values — no secrets, no PII. Public CSS is intended to be readable. |
| T-01-01-03 | Denial of Service | Sass build failure from malformed primitives | mitigate | `bundle exec jekyll build` MUST succeed before the plan is marked done; the build is the gate. CI also runs the build on push and blocks deploy on failure. |
| T-01-01-04 | Information Disclosure | Dark mode tokens with wrong contrast (accidental data exposure via unreadable text) | accept | WCAG AA contrast pre-verified in RESEARCH.md (all four light/dark pairings ≥ 5.68:1). No information loss risk. |

No high-severity threats. ASVS L1 satisfied by build-pipeline gate (T-01-01-03).
</threat_model>

<verification>
After both tasks complete, run:

```bash
bundle exec jekyll build
grep -i "5C4B8A" _site/assets/css/main.css | head -3
grep -i "FAFAF8" _site/assets/css/main.css | head -3
grep "0076df" _site/assets/css/main.css | grep -v -i "code" || echo "blue gone from non-code tokens"
```

Expected: `5C4B8A` and `FAFAF8` appear; `0076df` gone outside any residual code-bg derivation (Task 1 should eliminate it entirely).

Manual visual check (optional, deferred to phase verification): open `_site/index.html` locally — navbar active link should be plum, blockquote left border should be plum, page background should be warm off-white.
</verification>

<success_criteria>
- VIS-01 satisfied at the token layer: `--global-theme-color`, `--global-bg-color`, `--global-text-color`, `--global-text-color-light`, `--global-hover-color`, `--global-footer-bg-color`, `--global-divider-color`, `--global-card-bg-color` all updated in both light and dark blocks
- New `--font-sans` and `--font-serif` tokens are declared in both blocks (consumed by Plan 02)
- `bundle exec jekyll build` succeeds
- Compiled CSS contains `5C4B8A` and `FAFAF8`; old `0076df` no longer appears in semantic theme tokens
- All functional tokens (alert blocks, award highlight, back-to-top, code-bg derivation) still resolve correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation/01-01-SUMMARY.md` documenting:
- Files modified with line counts
- New primitives added (palette + font stacks)
- Token substitutions applied (light + dark)
- Verified grep results showing palette in compiled CSS
- Any deviations from plan (e.g., if `$code-bg-color-light` derivation was kept on `$blue-color`)
</output>
