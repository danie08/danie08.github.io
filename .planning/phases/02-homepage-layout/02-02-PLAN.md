---
phase: 02-homepage-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - _layouts/about.liquid
autonomous: true
requirements:
  - HOME-03

must_haves:
  truths:
    - "The selected papers list on the homepage is wrapped in a div.selected-papers-showcase element"
    - "The 'Main publications' h2 heading is NOT inside the wrapper — it sits above it"
    - "selected_papers.liquid is not modified — only about.liquid changes"
  artifacts:
    - path: "_layouts/about.liquid"
      provides: "Wrapper div around selected_papers include"
      contains: "selected-papers-showcase"
  key_links:
    - from: "_layouts/about.liquid div.selected-papers-showcase"
      to: "_includes/selected_papers.liquid"
      via: "{% include selected_papers.liquid %} nested inside wrapper div"
      pattern: "selected-papers-showcase"
---

<objective>
Add a `<div class="selected-papers-showcase">` wrapper around `{% include selected_papers.liquid %}` in `_layouts/about.liquid` (per D-04). This wrapper is the CSS scope anchor that Plan 03's SCSS block targets.

Purpose: Enable scoped SCSS to restyle only the homepage paper list — leaving the full `/publications/` page unaffected.
Output: One edited Liquid template with the wrapper div in place.
</objective>

<execution_context>
@/Users/daniela/Desktop/website/danie08.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniela/Desktop/website/danie08.github.io/.planning/phases/02-homepage-layout/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/daniela/Desktop/website/danie08.github.io/.planning/PROJECT.md
@/Users/daniela/Desktop/website/danie08.github.io/.planning/ROADMAP.md
@/Users/daniela/Desktop/website/danie08.github.io/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap selected_papers include in about.liquid</name>
  <files>_layouts/about.liquid</files>
  <read_first>
    _layouts/about.liquid — read the full file before editing. Target region is the "Selected papers" comment block (lines 59–65):
    ```liquid
    &lt;!-- Selected papers --&gt;
    {% if page.selected_papers %}
      &lt;h2&gt;
        &lt;a href="{{ '/publications/' | relative_url }}" class="section-link"&gt;Main publications&lt;/a&gt;
      &lt;/h2&gt;
      {% include selected_papers.liquid %}
    {% endif %}
    ```
    Also read _includes/selected_papers.liquid to confirm it is read-only and unchanged.
  </read_first>
  <action>
    Edit `_layouts/about.liquid` — change only the selected papers block.

    Current state (lines 59–65):
    ```liquid
    &lt;!-- Selected papers --&gt;
    {% if page.selected_papers %}
      &lt;h2&gt;
        &lt;a href="{{ '/publications/' | relative_url }}" class="section-link"&gt;Main publications&lt;/a&gt;
      &lt;/h2&gt;
      {% include selected_papers.liquid %}
    {% endif %}
    ```

    Target state after D-04:
    ```liquid
    &lt;!-- Selected papers --&gt;
    {% if page.selected_papers %}
      &lt;h2&gt;
        &lt;a href="{{ '/publications/' | relative_url }}" class="section-link"&gt;Main publications&lt;/a&gt;
      &lt;/h2&gt;
      &lt;div class="selected-papers-showcase"&gt;
        {% include selected_papers.liquid %}
      &lt;/div&gt;
    {% endif %}
    ```

    Exact changes:
    - Line 64 (`{% include selected_papers.liquid %}`): wrap with `&lt;div class="selected-papers-showcase"&gt;` before and `&lt;/div&gt;` after
    - The `&lt;h2&gt;` block (lines 61–63) stays OUTSIDE the wrapper — do not move it inside
    - Indentation: the `&lt;div&gt;` tag uses 2-space indent (matching the `&lt;h2&gt;` above it); the include inside uses 4-space indent (matching the existing style)
    - Do NOT touch `_includes/selected_papers.liquid` — it is read-only
    - Do NOT change any other part of about.liquid

    Per D-04.
  </action>
  <verify>
    <automated>grep -n 'selected-papers-showcase' /Users/daniela/Desktop/website/danie08.github.io/_layouts/about.liquid</automated>
    Expected: two lines — the opening `&lt;div class="selected-papers-showcase"&gt;` and closing `&lt;/div&gt;`

    <automated>grep -n 'selected_papers.liquid' /Users/daniela/Desktop/website/danie08.github.io/_layouts/about.liquid</automated>
    Expected: one line containing `{% include selected_papers.liquid %}` (still present, now wrapped)

    <automated>grep -n 'Main publications' /Users/daniela/Desktop/website/danie08.github.io/_layouts/about.liquid</automated>
    Expected: the h2/a line appears BEFORE (lower line number than) the selected-papers-showcase div

    <automated>bundle exec jekyll build --source /Users/daniela/Desktop/website/danie08.github.io --destination /Users/daniela/Desktop/website/danie08.github.io/_site 2>&amp;1 | tail -5</automated>
    Expected: build exits 0 with no Liquid errors.

    <automated>grep -c 'selected-papers-showcase' /Users/daniela/Desktop/website/danie08.github.io/_includes/selected_papers.liquid</automated>
    Expected: 0 (selected_papers.liquid must NOT contain the wrapper class — it is read-only)
  </verify>
  <acceptance_criteria>
    - `_layouts/about.liquid` contains `&lt;div class="selected-papers-showcase"&gt;` on a line before `{% include selected_papers.liquid %}`
    - `_layouts/about.liquid` contains `&lt;/div&gt;` on a line after `{% include selected_papers.liquid %}` (closing the wrapper)
    - The `&lt;h2&gt;` containing "Main publications" has a lower line number than the `selected-papers-showcase` div opening — heading is outside the wrapper
    - `_includes/selected_papers.liquid` is unchanged (grep for `selected-papers-showcase` returns 0)
    - `bundle exec jekyll build` exits 0
  </acceptance_criteria>
  <done>about.liquid wraps the selected_papers include in div.selected-papers-showcase, heading remains outside the wrapper, build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Liquid template → HTML output | Template change is static; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | `_layouts/about.liquid` wrapper div | accept | Static template edit; no runtime user input; wrapper adds a div with a hardcoded class name only |
</threat_model>

<verification>
After task completes:
1. `grep -n 'selected-papers-showcase' _layouts/about.liquid` shows opening and closing div tags
2. Line numbers confirm: h2 (Main publications) line number &lt; selected-papers-showcase div line number
3. `grep -c 'selected-papers-showcase' _includes/selected_papers.liquid` returns 0
4. `bundle exec jekyll build` exits 0
</verification>

<success_criteria>
- HOME-03 (partial): Wrapper div exists in about.liquid, scoping the bibliography output for Plan 03's SCSS
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage-layout/02-02-SUMMARY.md` using the summary template.
</output>
